<div class="header__options">
    {{-- @if(isset(auth()->user()->id))
        <a href="{{ route('cabinet') }}" class="header__register btn">
            <span>{{ __('Аккаунт') }}</span>
        </a>
        <a href="{{ route('logout') }}" class="header__register btn reg--trigger">
            <span>{{ __('Выйти') }}</span>
        </a>
    @else
        <button class="header__login btn login--trigger">
            <span>{{ __('Войти') }}</span>
        </button>
        <a class="header__register btn reg--trigger">
            <span>{{ __('Регистрация') }}</span>
        </a>
    @endif --}}
</div>
